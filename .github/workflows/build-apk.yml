name: Build Sudoku APK Pro V3

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04 # 锁定一个稳定的版本

    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4

    - name: 🐍 设置 Python 3.9
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'

    - name: ☕ 设置 JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 📦 安装构建依赖
      run: |
        sudo apt-get update
        sudo apt-get install -y git zip unzip wget autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev || \
        sudo apt-get install -y git zip unzip wget autoconf libtool pkg-config zlib1g-dev libncurses6-dev libncursesw6-dev libtinfo6 cmake libffi-dev libssl-dev
        
        python -m pip install --upgrade pip
        pip install cython==0.29.33 buildozer

    - name: 🏗️ 运行云端打包 (Buildozer)
      run: |
        buildozer android debug

    - name: 📤 上传打包好的 APK
      uses: actions/upload-artifact@v4
      with:
        name: Sudoku-3D-APK
        path: bin/*.apk
